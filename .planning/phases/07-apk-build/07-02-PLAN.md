---
phase: 07-apk-build
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified: []
autonomous: false
requirements:
  - UI-04

must_haves:
  truths:
    - "Keystore is backed up outside the repository"
    - "APK is uploaded to GitHub Releases as v0.7.0"
    - "Real device can install and run the APK"
  artifacts:
    - path: "~/invest-app-release.keystore"
      provides: "Keystore backup outside repo"
  key_links:
    - from: "invest-app/android/app/release.keystore"
      to: "~/invest-app-release.keystore"
      via: "file copy"
      pattern: "invest-app-release.keystore"
---

<objective>
Back up the keystore outside the repository, upload the APK to GitHub Releases, and verify the APK works on a real device.

Purpose: Complete the Phase 7 deliverables: keystore safety, distribution via GitHub Releases (per project memory), and real-device smoke test.
Output: Keystore backup at ~/invest-app-release.keystore, GitHub Release v0.7.0 with APK attached
</objective>

<execution_context>
@/Users/linmini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/linmini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-apk-build/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keystore backup and GitHub Release upload</name>
  <files></files>
  <action>
1. Back up the release keystore outside the repository:
   ```bash
   cp invest-app/android/app/release.keystore ~/invest-app-release.keystore
   ```
   Verify: `ls -la ~/invest-app-release.keystore` — file exists.
   Verify: `git check-ignore invest-app/android/app/release.keystore` — outputs the path (gitignored).

2. Copy the APK to the repo root for easy reference:
   ```bash
   cp invest-app/android/app/build/outputs/apk/release/app-release.apk invest-app-v0.7.0.apk
   ```

3. Create GitHub Release v0.7.0 with the APK attached (per project memory: always upload after phase):
   ```bash
   gh release create v0.7.0 invest-app-v0.7.0.apk \
     --repo Bala7651/invest-app \
     --title "v0.7.0 — Phase 7: APK Build" \
     --notes "Phase 7: APK Build complete.

   Changes:
   - Release keystore generated (PKCS12, RSA 2048-bit, 10000-day validity)
   - Gradle signingConfigs.release configured (no longer debug-signed)
   - APK secret audit script added (invest-app/scripts/audit-apk.sh)
   - Security audit passed: no API keys or secrets in JS bundle
   - Signing certificate: CN=Invest App (release, not debug)

   Install: Download the APK and sideload on any arm64 Android device."
   ```

4. Verify release was created: `gh release view v0.7.0 --repo Bala7651/invest-app`
  </action>
  <verify>
    <automated>ls ~/invest-app-release.keystore && gh release view v0.7.0 --repo Bala7651/invest-app 2>&1 | grep -q "v0.7.0" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Keystore backed up at ~/invest-app-release.keystore, GitHub Release v0.7.0 exists with APK attached</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real device smoke test</name>
  <action>
    Signed release APK (v0.7.0) with proper release keystore signing, secret audit verified clean.
    APK uploaded to GitHub Releases: https://github.com/Bala7651/invest-app/releases/tag/v0.7.0

    Verification steps:
    1. Download the APK from GitHub Releases (or use the local file `invest-app-v0.7.0.apk`)
    2. Install on a real Android device: `adb install invest-app-v0.7.0.apk`
    3. Open the app and verify:
       - Home screen loads with watchlist
       - Swipe left shows AI analysis page
       - Swipe right from home opens settings drawer
       - Tap a stock to see chart detail
    4. Confirm the app functions identically to previous debug-signed builds
  </action>
  <verify>User confirms APK installs and runs correctly on real device</verify>
  <done>User approved: APK installs, launches, and all screens work on real Android device</done>
  <resume-signal>Type "approved" if the APK works on device, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `~/invest-app-release.keystore` exists (backup outside repo)
2. `git check-ignore invest-app/android/app/release.keystore` confirms gitignored
3. GitHub Release v0.7.0 exists with APK attached
4. User confirms APK installs and runs on real Android device
</verification>

<success_criteria>
- Keystore backed up outside repository
- APK uploaded to GitHub Releases v0.7.0
- User confirms APK works on real device
</success_criteria>

<output>
After completion, create `.planning/phases/07-apk-build/07-02-SUMMARY.md`
</output>
