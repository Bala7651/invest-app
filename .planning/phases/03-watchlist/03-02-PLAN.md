---
phase: 03-watchlist
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - invest-app/src/features/watchlist/components/StockCard.tsx
  - invest-app/src/features/watchlist/components/SearchModal.tsx
  - invest-app/src/features/watchlist/components/EmptyWatchlist.tsx
  - invest-app/src/app/index.tsx
  - invest-app/src/__tests__/StockCard.test.ts
autonomous: true
requirements: [WTCH-01, WTCH-02, WTCH-03, WTCH-04, WTCH-05]

must_haves:
  truths:
    - "User can see a search bar on the home screen and tap it to open the search modal"
    - "User can type a stock code or name and see matching results instantly"
    - "User can tap '+' on a search result to add it to the watchlist; the button changes to a checkmark"
    - "User can swipe left on a watchlist card to reveal a red Delete button and remove the stock"
    - "User can long-press a card to drag-reorder it; new order persists"
    - "Each card shows stock code, name, current price, and price change (delta + percent)"
    - "When price is null, card shows dash with 'Waiting for market open'"
    - "Empty watchlist shows centered CTA with Add Stock button"
    - "Tapping a card navigates to detail/[symbol] page"
  artifacts:
    - path: "invest-app/src/features/watchlist/components/StockCard.tsx"
      provides: "Stock card row with price display, tap navigation"
      min_lines: 30
    - path: "invest-app/src/features/watchlist/components/SearchModal.tsx"
      provides: "Full-screen search modal with instant local filtering"
      min_lines: 50
    - path: "invest-app/src/features/watchlist/components/EmptyWatchlist.tsx"
      provides: "Empty state CTA component"
      min_lines: 15
    - path: "invest-app/src/app/index.tsx"
      provides: "WatchlistPage with ReorderableList, search bar, swipe-to-delete"
      min_lines: 60
  key_links:
    - from: "invest-app/src/app/index.tsx"
      to: "invest-app/src/features/watchlist/store/watchlistStore.ts"
      via: "useWatchlistStore hook for items, addItem, removeItem, reorderItems"
      pattern: "useWatchlistStore"
    - from: "invest-app/src/app/index.tsx"
      to: "invest-app/src/features/market/quoteStore.ts"
      via: "useQuoteStore hook for live quote data"
      pattern: "useQuoteStore"
    - from: "invest-app/src/features/watchlist/components/StockCard.tsx"
      to: "invest-app/src/features/market/quoteStore.ts"
      via: "Quote data passed as prop from parent"
      pattern: "quote.*price|change"
    - from: "invest-app/src/features/watchlist/components/SearchModal.tsx"
      to: "invest-app/src/features/watchlist/utils/searchStocks.ts"
      via: "filterStocks function for search results"
      pattern: "filterStocks"
---

<objective>
Build the watchlist UI: StockCard with swipe-to-delete, SearchModal with local filtering, EmptyWatchlist CTA, and rewrite WatchlistPage with ReorderableList and live quote data.

Purpose: Completes the user-facing watchlist experience — search, add, remove, reorder, and view live prices.
Output: StockCard.tsx, SearchModal.tsx, EmptyWatchlist.tsx, updated index.tsx, StockCard tests.
</objective>

<execution_context>
@/Users/linmini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/linmini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-watchlist/03-CONTEXT.md
@.planning/phases/03-watchlist/03-RESEARCH.md
@.planning/phases/03-watchlist/03-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan consumes -->

From invest-app/src/features/watchlist/store/watchlistStore.ts (after Plan 01):
```typescript
export interface WatchlistItem {
  id: number;
  symbol: string;
  name: string;
  sort_order: number;
}
export const useWatchlistStore = create<WatchlistState>((set, get) => ({
  items: WatchlistItem[];
  setItems: (items: WatchlistItem[]) => void;
  addItem: (symbol: string, name: string) => Promise<void>;
  removeItem: (id: number) => Promise<void>;
  loadFromDb: () => Promise<void>;
  reorderItems: (fromIndex: number, toIndex: number) => void;
}));
```

From invest-app/src/features/market/quoteStore.ts:
```typescript
interface Quote {
  symbol: string;
  name: string;
  price: number | null;
  prevClose: number;
  change: number;
  changePct: number;
  fetchedAt: number;
}
export const useQuoteStore = create<QuoteState>((set, get) => ({
  quotes: Record<string, Quote>;
  // ...
}));
```

From invest-app/src/features/watchlist/utils/searchStocks.ts (created in Plan 01):
```typescript
export interface StockEntry { code: string; name: string; }
export function filterStocks(query: string): StockEntry[];
```

From invest-app/src/app/detail/[symbol].tsx:
```typescript
// Already exists — placeholder detail page. Navigated to via:
router.push(`/detail/${symbol}`);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create StockCard, SearchModal, EmptyWatchlist components</name>
  <files>
    invest-app/src/features/watchlist/components/StockCard.tsx,
    invest-app/src/features/watchlist/components/SearchModal.tsx,
    invest-app/src/features/watchlist/components/EmptyWatchlist.tsx,
    invest-app/src/__tests__/StockCard.test.ts
  </files>
  <behavior>
    - StockCard with price 595.00, change +5.00, changePct +0.85 -> displays "595.00" and "+5.00 (+0.85%)"
    - StockCard with price null -> displays "—" and "Waiting for market open"
    - StockCard with negative change -3.50, changePct -0.58 -> displays "-3.50 (-0.58%)" with stock-down color
    - formatChange(5, 0.85) -> "+5.00 (+0.85%)"
    - formatChange(-3.5, -0.58) -> "-3.50 (-0.58%)"
  </behavior>
  <action>
    **1. Create StockCard.tsx** at `invest-app/src/features/watchlist/components/StockCard.tsx`:

    Props: `{ item: WatchlistItem, quote: Quote | undefined, onPress: () => void }`

    Layout (Bloomberg terminal row style — dense, data-focused):
    - Single row with `Pressable` wrapping. On press calls `onPress` (navigates to detail).
    - Left side: stock code (text-primary, font-semibold) and name (text-muted, smaller) stacked vertically
    - Right side: current price (text-text, font-semibold, right-aligned) and change string below it
    - Price display: `quote?.price != null ? quote.price.toFixed(2) : '—'`
    - Change display: `quote?.price != null ? formatChange(quote.change, quote.changePct) : 'Waiting for market open'`
    - Change color: `quote?.change >= 0 ? 'text-stock-up' : 'text-stock-down'` (when no quote, use text-muted)
    - Background: `bg-surface`, border: `border-border`, rounded corners, padding
    - Export `formatChange` as a named export for testing

    **2. Create SearchModal.tsx** at `invest-app/src/features/watchlist/components/SearchModal.tsx`:

    Props: `{ visible: boolean, onClose: () => void }`

    - Full-screen Modal (`animationType="slide"`, `presentationStyle="fullScreen"`)
    - Header row: "Search Stocks" title + "Close" button (calls onClose)
    - TextInput with cyberpunk styling (bg-surface, text-text, border-border), placeholder "Search by code or name..."
    - Auto-focus on mount, keyboard dismiss on scroll
    - Results rendered in FlatList below the input
    - Each result row: code + name on left, '+' button on right
    - If stock is already in watchlist (check `useWatchlistStore.getState().items`), show checkmark icon instead of '+'
    - Tapping '+' calls `useWatchlistStore.getState().addItem(code, name)`, button instantly changes to checkmark
    - Use `filterStocks(query)` from `../utils/searchStocks` for filtering
    - Empty state when no query: show hint text "Type a stock code or company name"

    **3. Create EmptyWatchlist.tsx** at `invest-app/src/features/watchlist/components/EmptyWatchlist.tsx`:

    Props: `{ onAddPress: () => void }`

    - Centered vertically in the available space
    - Text: "No stocks yet" (text-muted, text-lg)
    - Subtitle: "Add stocks to start tracking prices" (text-muted, text-sm)
    - Button: "Add Stock" styled with bg-primary, text-bg, rounded, padding — calls onAddPress

    **4. Write StockCard.test.ts:**
    - Test `formatChange` pure function with positive, negative, and zero values
    - Test price display logic: null price -> '—', valid price -> formatted string
    - These are unit tests of the formatting logic, not render tests

    NativeWind classes to use: bg-surface, bg-bg, text-text, text-primary, text-muted, text-stock-up, text-stock-down, border-border, rounded-lg
  </action>
  <verify>
    <automated>cd /Users/linmini/invest/invest-app && npx jest --testPathPattern="StockCard" --no-coverage</automated>
  </verify>
  <done>
    - StockCard renders stock code, name, price, and formatted change
    - Null price shows dash and "Waiting for market open"
    - SearchModal opens full-screen with instant local search
    - Already-added stocks show checkmark instead of '+' button
    - EmptyWatchlist shows centered CTA
    - formatChange tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite WatchlistPage with ReorderableList, search bar, and swipe-to-delete</name>
  <files>invest-app/src/app/index.tsx</files>
  <action>
    **Install dependencies first:**
    ```bash
    cd /Users/linmini/invest/invest-app && npx expo install react-native-reorderable-list expo-haptics
    ```

    **Rewrite the WatchlistPage function in index.tsx:**

    1. **State and hooks:**
    - `const items = useWatchlistStore(s => s.items)`
    - `const quotes = useQuoteStore(s => s.quotes)`
    - `const [searchVisible, setSearchVisible] = useState(false)`
    - `const router = useRouter()`

    2. **Search bar** (below MarketStatusBar, above the list):
    - A `Pressable` styled to look like a search input (bg-surface, border-border, rounded, text-muted placeholder text)
    - Tap opens SearchModal (`setSearchVisible(true)`)
    - NOT a real TextInput — just a visual trigger for the modal

    3. **List rendering:**
    - If `items.length === 0`: render `<EmptyWatchlist onAddPress={() => setSearchVisible(true)} />`
    - Otherwise: render `ReorderableList` from `react-native-reorderable-list`
    - Each item wrapped in: `ReorderableListItem` > `ReanimatedSwipeable` (right action = red Delete) > `Pressable` (onLongPress = drag, onPress = navigate)
    - `keyExtractor: (item) => item.symbol`

    4. **Swipe-to-delete per item:**
    - Import `Swipeable` from `react-native-gesture-handler/ReanimatedSwipeable`
    - `renderRightActions`: render a red `bg-stock-down` view with "Delete" text, width 80
    - `onSwipeableOpen`: call `useWatchlistStore.getState().removeItem(item.id)`
    - `rightThreshold={40}`, `friction={2}`

    5. **Drag-to-reorder:**
    - Import `ReorderableList`, `ReorderableListItem`, `useReorderableDrag`, `reorderItems` from `react-native-reorderable-list`
    - Each row: `const drag = useReorderableDrag()`
    - `onLongPress`: call `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium)` (wrap in try/catch), then `drag()`
    - `onReorder={({ fromIndex, toIndex }) => useWatchlistStore.getState().reorderItems(fromIndex, toIndex)}`

    6. **Card content:**
    - Render `<StockCard item={item} quote={quotes[item.symbol]} onPress={() => router.push(`/detail/${item.symbol}`)} />`

    7. **SearchModal:**
    - Render `<SearchModal visible={searchVisible} onClose={() => setSearchVisible(false)} />` at the bottom of the component

    8. **Keep the AnalysisPage function and PagerView structure unchanged.** WatchlistPage is page 0, AnalysisPage is page 1.

    9. **Keep the existing MarketStatusBar in the header.**

    **Gesture nesting order (critical — from RESEARCH.md):**
    `ReorderableListItem` (outermost) > `Swipeable` > `Pressable` with onLongPress for drag
    This order ensures ReorderableListItem scopes the drag gesture correctly, and Swipeable handles horizontal pan without conflict.
  </action>
  <verify>
    <automated>cd /Users/linmini/invest/invest-app && npx jest --no-coverage && npx tsc --noEmit</automated>
  </verify>
  <done>
    - WatchlistPage renders real stock cards from watchlistStore
    - Search bar opens SearchModal on tap
    - Cards show live price data from quoteStore
    - Swipe left reveals red Delete button, swiping fully calls removeItem
    - Long-press activates drag-to-reorder with haptic feedback
    - Reorder persists via watchlistStore.reorderItems
    - Empty state shows EmptyWatchlist CTA
    - Tapping a card navigates to /detail/[symbol]
    - All tests pass, tsc clean
  </done>
</task>

</tasks>

<verification>
- `npx jest --no-coverage` — all tests pass (existing + new)
- `npx tsc --noEmit` — zero type errors
- WatchlistPage renders with real data from stores
- Search, add, remove, reorder all functional
- Navigation to detail page works
</verification>

<success_criteria>
- Home screen shows real watchlist cards with live price data
- Search modal opens, filters stocks by code/name, adds with '+' button
- Swipe-to-delete removes stocks with visual feedback
- Long-press drag-to-reorder works with haptic feedback
- Empty state shows CTA to add stocks
- Tapping card navigates to detail page
- All formatting handles null prices gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/03-watchlist/03-02-SUMMARY.md`
</output>
