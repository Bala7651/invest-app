---
phase: 02-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - invest-app/src/services/stockService.ts
  - invest-app/src/features/market/marketHours.ts
  - invest-app/src/features/market/holidays2026.ts
  - invest-app/src/__tests__/stockService.test.ts
  - invest-app/src/__tests__/marketHours.test.ts
autonomous: true
requirements: [DATA-01, DATA-05]

must_haves:
  truths:
    - "getQuotes(symbols) returns typed TWSEQuote array with price, prevClose, symbol, name fields"
    - "Two consecutive getQuotes calls are spaced at least 2 seconds apart"
    - "Internal _fetchQuotes and _queue are not exported (bypass impossible)"
    - "isMarketOpen() returns true only during Mon-Fri 09:00-13:30 Asia/Taipei"
    - "isHoliday() returns true for all 2026 Taiwan public holidays"
    - "TWSE z field '-' sentinel is parsed as null, not NaN"
  artifacts:
    - path: "invest-app/src/services/stockService.ts"
      provides: "FIFO request queue + TWSE fetch client"
      exports: ["getQuotes", "TWSEQuote"]
    - path: "invest-app/src/features/market/marketHours.ts"
      provides: "Pure market-hours and holiday detection functions"
      exports: ["isMarketOpen", "isHoliday", "computeStatus"]
    - path: "invest-app/src/features/market/holidays2026.ts"
      provides: "Hardcoded 2026 TWSE holiday list"
      exports: ["HOLIDAYS_2026"]
    - path: "invest-app/src/__tests__/stockService.test.ts"
      provides: "Unit tests for queue bypass rejection and spacing"
    - path: "invest-app/src/__tests__/marketHours.test.ts"
      provides: "Unit tests for isMarketOpen, isHoliday, computeStatus"
  key_links:
    - from: "invest-app/src/services/stockService.ts"
      to: "https://mis.twse.com.tw/stock/api/getStockInfo.jsp"
      via: "fetch with AbortSignal.timeout"
      pattern: "mis\\.twse\\.com\\.tw.*getStockInfo"
    - from: "invest-app/src/features/market/marketHours.ts"
      to: "invest-app/src/features/market/holidays2026.ts"
      via: "import HOLIDAYS_2026"
      pattern: "HOLIDAYS_2026"
---

<objective>
Create the TWSE OpenAPI client with FIFO rate-limit queue and pure market-hours/holiday detection functions, fully unit tested.

Purpose: This is the foundation data layer — all stock data flows through StockService, and all time-based guards use marketHours. Both must be correct and tested before any polling or UI can be built.
Output: stockService.ts, marketHours.ts, holidays2026.ts, and their unit tests — all passing.
</objective>

<execution_context>
@/Users/linmini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/linmini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-layer/02-CONTEXT.md
@.planning/phases/02-data-layer/02-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Existing project interfaces the executor needs -->

From invest-app/src/features/watchlist/store/watchlistStore.ts:
```typescript
export interface WatchlistItem {
  id: number;
  symbol: string;
  name: string;
  sort_order: number;
}
```

From invest-app/tailwind.config.js:
```javascript
colors: {
  bg: '#050508',
  surface: '#0D0D14',
  border: '#1A1A2E',
  primary: '#4D7CFF',
  secondary: '#8B5CF6',
  'stock-up': '#00E676',
  'stock-down': '#FF1744',
  text: '#E0E0E0',
  muted: '#6B7280',
}
```

Test infrastructure: jest-expo preset configured in package.json, tests in src/__tests__/.
Test run command: `npm test` from invest-app/ directory.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create StockService with FIFO queue, TWSE fetch, and unit tests</name>
  <files>invest-app/src/services/stockService.ts, invest-app/src/__tests__/stockService.test.ts</files>
  <behavior>
    - Test: _fetchQuotes is NOT exported from stockService (bypass impossible)
    - Test: _queue is NOT exported from stockService (bypass impossible)
    - Test: getQuotes is exported and is the only fetch-triggering export
    - Test: No exports start with underscore (no private escape hatches)
    - Test: TWSEQuote type has required fields (symbol, name, price, prevClose, open, high, low, volume, updatedAt)
    - Test: parseSentinel('-') returns null, parseSentinel('123.45') returns 123.45, parseSentinel('') returns null
    - Test: getQuotes formats symbols as pipe-delimited tse_XXXX.tw string (mock fetch to verify URL)
    - Test: Two sequential enqueue calls execute with >= 2000ms spacing (mock Date.now)
  </behavior>
  <action>
    1. Create `invest-app/src/__tests__/stockService.test.ts` with RED tests:
       - Export bypass tests: verify _fetchQuotes, _queue not in module exports
       - parseSentinel tests: '-' -> null, '' -> null, '123.45' -> 123.45, 'abc' -> null
       - URL format test: mock global.fetch, call getQuotes(['2330', '2317']), assert URL contains 'tse_2330.tw|tse_2317.tw'
       - Queue spacing test: mock Date.now, enqueue two calls, verify >= 2000ms gap between executions

    2. Create `invest-app/src/services/stockService.ts`:
       - RequestQueue class (private, not exported): FIFO queue with 2s minimum spacing between requests
         - `enqueue<T>(fn: () => Promise<T>): Promise<T>` — public method
         - `_drain()` — private, processes queue items sequentially
         - `lastRequestTime` tracks timing for 2s enforcement
       - Module-level singleton `_queue` (not exported)
       - `_fetchQuotes(symbols: string[])` (not exported): builds mis.twse.com.tw URL, fetches with 10s timeout, parses response
         - Format: `tse_${symbol}.tw` joined by `|` (TSE default; OTC support deferred)
         - Handles `z === '-'` sentinel via parseSentinel helper
         - Wraps with retry-once logic: catch -> wait 5s -> retry -> catch -> return null
       - `export async function getQuotes(symbols: string[]): Promise<TWSEQuote[]>` — the ONLY public fetch function
       - `export interface TWSEQuote { symbol, name, price: number | null, prevClose, open: number | null, high: number | null, low: number | null, volume, updatedAt }`
       - `export function parseSentinel(val: string): number | null` — exported for testing; converts '-' and '' to null, otherwise parseFloat

    3. Run tests: `npm test -- --testPathPattern="stockService"` — all must pass GREEN.
  </action>
  <verify>
    <automated>cd /Users/linmini/invest/invest-app && npm test -- --testPathPattern="stockService" --passWithNoTests</automated>
  </verify>
  <done>StockService exports only getQuotes and TWSEQuote and parseSentinel. FIFO queue enforces 2s spacing. All tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create marketHours pure functions, holidays list, and unit tests</name>
  <files>invest-app/src/features/market/marketHours.ts, invest-app/src/features/market/holidays2026.ts, invest-app/src/__tests__/marketHours.test.ts</files>
  <behavior>
    - Test: isMarketOpen returns true at 10:00 Taipei on a Tuesday (non-holiday)
    - Test: isMarketOpen returns false at 14:00 Taipei on a Tuesday
    - Test: isMarketOpen returns false at 10:00 Taipei on a Saturday
    - Test: isMarketOpen returns false at 10:00 Taipei on a Sunday
    - Test: isMarketOpen returns false at 08:59 Taipei on a Tuesday (one minute before open)
    - Test: isMarketOpen returns false at 13:30 Taipei on a Tuesday (market closes AT 13:30)
    - Test: isMarketOpen returns true at 13:29 Taipei on a Tuesday (one minute before close)
    - Test: isHoliday returns true for '2026-02-17' (Lunar New Year)
    - Test: isHoliday returns false for '2026-03-18' (regular Wednesday)
    - Test: HOLIDAYS_2026 array has at least 15 entries
    - Test: computeStatus returns { open: true, label: 'Open ...' } during market hours
    - Test: computeStatus returns { open: false, label: 'Closed ...' } outside market hours
  </behavior>
  <action>
    1. Create `invest-app/src/__tests__/marketHours.test.ts` with RED tests:
       - Use explicit Date objects for Taipei times. Helper: create UTC date that corresponds to desired Taipei time (UTC = Taipei - 8h).
       - Test all isMarketOpen edge cases listed in behavior section
       - Test isHoliday with known holidays and known non-holidays
       - Test computeStatus label format patterns

    2. Create `invest-app/src/features/market/holidays2026.ts`:
       - Export `HOLIDAYS_2026: string[]` with all 18 dates from RESEARCH.md
       - Include comment with source URL for annual verification

    3. Create `invest-app/src/features/market/marketHours.ts`:
       - `export function isMarketOpen(now = new Date()): boolean` — converts to Taipei time, checks weekday + not holiday + 09:00 <= time < 13:30
       - `export function isHoliday(date: Date): boolean` — converts to Taipei ISO date string, checks against HOLIDAYS_2026
       - `export function computeStatus(now = new Date()): { open: boolean; label: string }` — returns status with countdown label:
         - Open: "Open · Xh Ym to close"
         - Closed: "Closed · opens HH:MM" (next market open time)
       - Internal helper `toTaipeiDate(utc: Date): Date` — uses toLocaleString with timeZone: 'Asia/Taipei'
       - All functions accept optional `now` parameter for testability (no Date mocking needed)

    4. Run tests: `npm test -- --testPathPattern="marketHours"` — all must pass GREEN.
  </action>
  <verify>
    <automated>cd /Users/linmini/invest/invest-app && npm test -- --testPathPattern="marketHours" --passWithNoTests</automated>
  </verify>
  <done>isMarketOpen correctly identifies market hours including weekends and holidays. computeStatus returns formatted countdown labels. All tests green.</done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:
```bash
cd /Users/linmini/invest/invest-app && npm test
```
All tests pass (existing 15 from Phase 1 + new stockService + marketHours tests).
</verification>

<success_criteria>
- getQuotes fetches from mis.twse.com.tw with correct URL format
- FIFO queue enforces >= 2s spacing between requests
- No internal functions (_fetchQuotes, _queue) are exported
- isMarketOpen correctly returns true only during Mon-Fri 09:00-13:30 Asia/Taipei, excluding holidays
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer/02-01-SUMMARY.md`
</output>
