---
phase: 05-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - invest-app/src/features/settings/store/settingsStore.ts
  - invest-app/src/features/settings/store/__tests__/settingsStore.test.ts
  - invest-app/src/app/_layout.tsx
autonomous: true
requirements: [SETT-02, SETT-03, SETT-04]

must_haves:
  truths:
    - "API key saved via settingsStore persists in expo-secure-store keychain"
    - "API key is never written to SQLite or AsyncStorage"
    - "Model name defaults to MiniMax-M2.5 and can be changed"
    - "Base URL defaults to https://api.minimax.io/v1 and can be changed"
    - "API key hydrates from secure store on app startup before settings screen is reachable"
  artifacts:
    - path: "invest-app/src/features/settings/store/settingsStore.ts"
      provides: "Extended Zustand store with apiKey, modelName, baseUrl state + SecureStore actions"
      exports: ["useSettingsStore", "GlowLevel"]
    - path: "invest-app/src/features/settings/store/__tests__/settingsStore.test.ts"
      provides: "Unit tests for saveApiKey, loadFromSecureStore, deleteApiKey, setModelName, setBaseUrl"
      min_lines: 40
    - path: "invest-app/src/app/_layout.tsx"
      provides: "loadFromSecureStore call in hydration effect"
  key_links:
    - from: "invest-app/src/features/settings/store/settingsStore.ts"
      to: "expo-secure-store"
      via: "SecureStore.setItemAsync / getItemAsync / deleteItemAsync"
      pattern: "SecureStore\\.(set|get|delete)ItemAsync"
    - from: "invest-app/src/app/_layout.tsx"
      to: "invest-app/src/features/settings/store/settingsStore.ts"
      via: "loadFromSecureStore call after migration success"
      pattern: "loadFromSecureStore"
---

<objective>
Install expo-secure-store, extend the existing settingsStore with API key / model name / base URL state backed by the platform keychain, add hydration in _layout.tsx, and write unit tests.

Purpose: The AI analysis feature (Phase 6) needs a securely stored API key. This plan provides the data layer and ensures the key never appears in plaintext outside the keychain.
Output: Working settingsStore with SecureStore integration, hydration wired, tests passing.
</objective>

<execution_context>
@/Users/linmini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/linmini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-settings/05-CONTEXT.md
@.planning/phases/05-settings/05-RESEARCH.md

<interfaces>
<!-- Existing settingsStore to extend -->
From invest-app/src/features/settings/store/settingsStore.ts:
```typescript
import { create } from 'zustand';
export type GlowLevel = 'subtle' | 'medium' | 'heavy';
interface SettingsState {
  glowLevel: GlowLevel;
  setGlowLevel: (level: GlowLevel) => void;
}
export const useSettingsStore = create<SettingsState>((set) => ({
  glowLevel: 'subtle',
  setGlowLevel: (level) => set({ glowLevel: level }),
}));
```

From invest-app/src/app/_layout.tsx (hydration pattern):
```typescript
// After migration succeeds:
useWatchlistStore.getState().loadFromDb().then(() => {
  const symbols = useWatchlistStore.getState().items.map(i => i.symbol);
  if (symbols.length > 0 && isMarketOpen()) {
    useQuoteStore.getState().startPolling(symbols);
  }
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install expo-secure-store and extend settingsStore</name>
  <files>invest-app/src/features/settings/store/settingsStore.ts, invest-app/src/features/settings/store/__tests__/settingsStore.test.ts</files>
  <behavior>
    - saveApiKey('test-key-1234') calls SecureStore.setItemAsync('minimax_api_key', 'test-key-1234') and sets state.apiKey to 'test-key-1234'
    - loadFromSecureStore reads from SecureStore.getItemAsync('minimax_api_key') and populates state.apiKey; returns '' if null
    - deleteApiKey calls SecureStore.deleteItemAsync('minimax_api_key') and clears state.apiKey to ''
    - setModelName('custom-model') updates state.modelName to 'custom-model'
    - setBaseUrl('https://custom.url/v1') updates state.baseUrl to 'https://custom.url/v1'
    - Initial modelName is 'MiniMax-M2.5', initial baseUrl is 'https://api.minimax.io/v1', initial apiKey is ''
    - No SQLite or AsyncStorage imports exist in the file (SETT-04 verification)
  </behavior>
  <action>
    1. Run `npx expo install expo-secure-store` in invest-app/
    2. Create test file at `invest-app/src/features/settings/store/__tests__/settingsStore.test.ts`:
       - Mock `expo-secure-store` with jest.mock: provide setItemAsync, getItemAsync, deleteItemAsync as jest.fn()
       - Write tests for all behaviors listed above
       - Reset store state between tests using useSettingsStore.setState()
    3. Extend `invest-app/src/features/settings/store/settingsStore.ts`:
       - Import * as SecureStore from 'expo-secure-store'
       - Add apiKey (string, default ''), modelName (string, default 'MiniMax-M2.5'), baseUrl (string, default 'https://api.minimax.io/v1') to state
       - Add loadFromSecureStore, saveApiKey, deleteApiKey async actions using SecureStore
       - Add setModelName, setBaseUrl synchronous actions
       - Keep existing glowLevel and setGlowLevel unchanged
       - Use constant `API_KEY_STORE_KEY = 'minimax_api_key'` for the keychain key name
    4. Also persist modelName and baseUrl to SecureStore (keys: 'minimax_model_name', 'minimax_base_url') so they survive app restart. These are not secrets but user should not re-enter them. Load them in loadFromSecureStore alongside the API key.
  </action>
  <verify>
    <automated>cd /Users/linmini/invest/invest-app && npx jest --testPathPattern=settingsStore --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>All settingsStore tests pass. Store has apiKey/modelName/baseUrl state with SecureStore persistence. No SQLite or AsyncStorage imports in the store file.</done>
</task>

<task type="auto">
  <name>Task 2: Wire settingsStore hydration in _layout.tsx</name>
  <files>invest-app/src/app/_layout.tsx</files>
  <action>
    1. Import useSettingsStore from '../features/settings/store/settingsStore'
    2. In the existing hydration useEffect (the one gated on `success`), add `useSettingsStore.getState().loadFromSecureStore()` call. This should run in parallel with loadFromDb — use Promise.all or just fire it alongside. Since settings hydration is independent of watchlist, it does not need to block polling start.
    3. The call pattern: after `if (!success) return;`, add the loadFromSecureStore call before or alongside the watchlistStore.loadFromDb().then(...) chain. Example:
       ```
       useSettingsStore.getState().loadFromSecureStore();
       useWatchlistStore.getState().loadFromDb().then(() => { ... });
       ```
    4. Do NOT modify any other logic in _layout.tsx — keep existing polling, AppState, migration, and error handling unchanged.
  </action>
  <verify>
    <automated>cd /Users/linmini/invest/invest-app && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>_layout.tsx imports settingsStore, calls loadFromSecureStore on app start. TypeScript compiles without errors. Settings values are available in Zustand state by the time user navigates to any screen.</done>
</task>

</tasks>

<verification>
1. `npx jest --testPathPattern=settingsStore` — all unit tests green
2. `npx tsc --noEmit` — no type errors across codebase
3. `expo-secure-store` appears in package.json dependencies
4. grep settingsStore.ts for 'SQLite\|AsyncStorage' returns zero matches (SETT-04)
</verification>

<success_criteria>
- settingsStore exports apiKey, modelName, baseUrl state with SecureStore-backed persistence
- Unit tests verify save/load/delete cycle for API key
- _layout.tsx hydrates settings on app start
- API key is never stored in SQLite or plaintext files
</success_criteria>

<output>
After completion, create `.planning/phases/05-settings/05-01-SUMMARY.md`
</output>
