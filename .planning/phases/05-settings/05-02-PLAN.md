---
phase: 05-settings
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - invest-app/src/app/settings.tsx
  - invest-app/src/app/index.tsx
  - invest-app/src/features/settings/components/HamburgerDrawer.tsx
  - invest-app/src/features/settings/components/ApiKeyInput.tsx
  - invest-app/src/features/settings/components/GlowPillSelector.tsx
autonomous: false
requirements: [SETT-01, SETT-05]

must_haves:
  truths:
    - "User can tap hamburger icon in top-left of home screen to open a slide-in drawer"
    - "Drawer contains Settings item that navigates to the settings screen"
    - "Drawer closes via swipe left or tap outside on dimmed overlay"
    - "Settings screen has API Configuration section with API key field, model name field, base URL field"
    - "Settings screen has Display section with glow intensity pill selector (Subtle/Medium/Heavy)"
    - "API key displays masked with last 4 chars visible; eye icon reveals full key"
    - "Auto-save on blur for all fields with brief Saved toast"
    - "Test button verifies API key with lightweight API call"
    - "Clear button deletes API key with confirmation prompt"
  artifacts:
    - path: "invest-app/src/features/settings/components/HamburgerDrawer.tsx"
      provides: "ReanimatedDrawerLayout wrapper with drawer content and hamburger trigger"
      min_lines: 30
    - path: "invest-app/src/features/settings/components/ApiKeyInput.tsx"
      provides: "Masked API key input with eye toggle, Test button, Clear button, auto-save on blur"
      min_lines: 60
    - path: "invest-app/src/features/settings/components/GlowPillSelector.tsx"
      provides: "Three-pill selector reusing TimeframeSelector pattern for glow intensity"
      min_lines: 30
    - path: "invest-app/src/app/settings.tsx"
      provides: "Full settings screen with API Configuration and Display sections"
      min_lines: 40
    - path: "invest-app/src/app/index.tsx"
      provides: "Home screen with hamburger icon and drawer wrapper"
  key_links:
    - from: "invest-app/src/features/settings/components/ApiKeyInput.tsx"
      to: "invest-app/src/features/settings/store/settingsStore.ts"
      via: "useSettingsStore for saveApiKey, deleteApiKey, apiKey state"
      pattern: "useSettingsStore"
    - from: "invest-app/src/features/settings/components/GlowPillSelector.tsx"
      to: "invest-app/src/features/settings/store/settingsStore.ts"
      via: "useSettingsStore for glowLevel, setGlowLevel"
      pattern: "useSettingsStore"
    - from: "invest-app/src/app/index.tsx"
      to: "invest-app/src/features/settings/components/HamburgerDrawer.tsx"
      via: "HamburgerDrawer wraps PagerView content"
      pattern: "HamburgerDrawer"
    - from: "invest-app/src/features/settings/components/HamburgerDrawer.tsx"
      to: "react-native-gesture-handler/ReanimatedDrawerLayout"
      via: "ReanimatedDrawerLayout component"
      pattern: "ReanimatedDrawerLayout"
---

<objective>
Build the Settings screen UI with masked API key input, model name/base URL fields, glow pill selector, and hamburger drawer on the home screen.

Purpose: Users need a way to enter their MiniMax API key and configure AI settings so Phase 6 can function. The hamburger drawer provides the navigation entry point.
Output: Complete settings UI with all input fields, drawer navigation from home screen.
</objective>

<execution_context>
@/Users/linmini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/linmini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-settings/05-CONTEXT.md
@.planning/phases/05-settings/05-RESEARCH.md
@.planning/phases/05-settings/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — settingsStore exports -->
From invest-app/src/features/settings/store/settingsStore.ts:
```typescript
export type GlowLevel = 'subtle' | 'medium' | 'heavy';
export const useSettingsStore = create<SettingsState>((set) => ({
  glowLevel: GlowLevel;
  apiKey: string;          // '' = not set
  modelName: string;       // default 'MiniMax-M2.5'
  baseUrl: string;         // default 'https://api.minimax.io/v1'
  setGlowLevel: (level: GlowLevel) => void;
  loadFromSecureStore: () => Promise<void>;
  saveApiKey: (key: string) => Promise<void>;
  deleteApiKey: () => Promise<void>;
  setModelName: (name: string) => void;
  setBaseUrl: (url: string) => void;
}));
```

<!-- TimeframeSelector pill pattern to reuse for GlowPillSelector -->
From invest-app/src/features/charts/components/TimeframeSelector.tsx:
```typescript
// Uses Reanimated animated sliding highlight behind pills
// TIMEFRAMES array + active prop + onSelect callback
// Animated.View with useAnimatedStyle for translateX
// handleLayout measures container, divides by item count
```

<!-- Home screen structure -->
From invest-app/src/app/index.tsx:
```typescript
// PagerView with key="0" WatchlistPage and key="1" AnalysisPage
// WatchlistPage has header: "Watchlist" text + "+ Add" button
// Need to add hamburger icon to header left side
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create settings components and update settings screen</name>
  <files>invest-app/src/features/settings/components/ApiKeyInput.tsx, invest-app/src/features/settings/components/GlowPillSelector.tsx, invest-app/src/app/settings.tsx</files>
  <action>
    **ApiKeyInput.tsx** — Masked API key input component:
    - State: `isRevealed` (boolean), `inputValue` (string), `isTesting` (boolean), `testResult` ('success' | 'error' | null), `showSavedToast` (boolean)
    - Display logic: when not focused, show `maskKey(apiKey)` where maskKey returns '........' + last 4 chars (or full key if <= 4 chars). When focused, show actual inputValue for editing.
    - Use `defaultValue` + `onChangeText` pattern (uncontrolled) to avoid secureTextEntry cursor jump pitfall.
    - Eye icon (Pressable with text "Show"/"Hide") toggles `isRevealed`. When revealed, secureTextEntry={false}.
    - On blur: call `settingsStore.saveApiKey(inputValue)`, show brief "Saved" toast (Animated.View opacity fade, 1.5s duration, position absolute top of component).
    - "Test" button: calls testConnection function — POST to `${baseUrl}/chat/completions` with `max_tokens: 1`, model from store, Bearer apiKey. Show spinner during test, green checkmark text on success, red X text on error. Use `isTesting` state to disable button during call.
    - "Clear" button (trash icon or text): shows Alert.alert confirmation prompt, on confirm calls `settingsStore.deleteApiKey()`.
    - Styling: bg-surface, border-border, text-text, text-muted for labels. Input has neon primary border on focus.

    **GlowPillSelector.tsx** — Reuse TimeframeSelector pill pattern:
    - Define `GLOW_LEVELS = ['subtle', 'medium', 'heavy'] as const` and labels `['Subtle', 'Medium', 'Heavy']`.
    - Copy the sliding highlight animation pattern from TimeframeSelector: onLayout measures width, Animated.View translateX on press.
    - Props: `active: GlowLevel`, `onSelect: (level: GlowLevel) => void`.
    - Same cyberpunk styling: bg-surface background, bg-primary sliding highlight, text-bg for active, text-muted for inactive.

    **settings.tsx** — Replace placeholder with full settings screen:
    - Back button in header (keep existing router.back() pattern).
    - Title: "Settings".
    - Section 1: "API Configuration" — contains ApiKeyInput, then TextInput for model name (label "AI Model", placeholder "MiniMax-M2.5", auto-save on blur via setModelName), then TextInput for base URL (label "Base URL", placeholder "https://api.minimax.io/v1", auto-save on blur via setBaseUrl). Each field in its own row with label above.
    - Section 2: "Display" — contains GlowPillSelector wired to settingsStore.glowLevel / setGlowLevel.
    - All fields read initial values from useSettingsStore.
    - Model name and base URL fields: on blur, call respective store setters + show brief "Saved" toast.
    - Use ScrollView for the content area (keyboard may push content).
    - Wrap with KeyboardAvoidingView (behavior="padding" on iOS, undefined on Android).
  </action>
  <verify>
    <automated>cd /Users/linmini/invest/invest-app && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Settings screen renders with API key masked input (eye toggle, Test button, Clear button), model name field, base URL field, and glow intensity pill selector. All fields read from and write to settingsStore. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create HamburgerDrawer and integrate into home screen</name>
  <files>invest-app/src/features/settings/components/HamburgerDrawer.tsx, invest-app/src/app/index.tsx</files>
  <action>
    **HamburgerDrawer.tsx** — Drawer wrapper component:
    - Import `ReanimatedDrawerLayout` and `DrawerType` from `react-native-gesture-handler/ReanimatedDrawerLayout`.
    - Props: `children: React.ReactNode` (the home screen content to wrap).
    - Use `useRef` for drawer ref. Export a way for children to trigger open — use React context or forwardRef. Simplest: export a `DrawerContext` with `openDrawer` function, or expose ref via forwardRef.
    - Recommended approach: `HamburgerDrawer` is a wrapper that provides `openDrawer` via React context (`DrawerContext`). Children call `useDrawer().openDrawer()`.
    - DrawerContent: a View with bg-surface, pt-12 (safe area offset), px-4. Contains a single Pressable "Settings" item with gear icon (text). On press: close drawer, then router.push('/settings'). Style the Settings item with text-text text-base, add a subtle border-b border-border below it.
    - ReanimatedDrawerLayout props: `drawerType={DrawerType.FRONT}`, `drawerWidth={260}`, `overlayColor="rgba(0,0,0,0.6)"`, `edgeWidth={30}` (to avoid conflicting with PagerView horizontal swipe — per research pitfall 6).
    - renderNavigationView returns DrawerContent.

    **index.tsx** — Add hamburger icon and drawer wrapper:
    - Import HamburgerDrawer and useDrawer hook from settings components.
    - In WatchlistPage header row: add a hamburger icon (three horizontal lines using Text or a simple View-based icon) as a Pressable on the LEFT side, before "Watchlist" text. On press: call openDrawer from context.
    - Wrap the PagerView in HomeScreen with HamburgerDrawer:
      ```
      <HamburgerDrawer>
        <PagerView ...>
          ...
        </PagerView>
      </HamburgerDrawer>
      ```
    - Keep ALL existing WatchlistPage and AnalysisPage logic unchanged. Only add the hamburger button and drawer wrapper.
  </action>
  <verify>
    <automated>cd /Users/linmini/invest/invest-app && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Home screen shows hamburger icon in top-left. Tapping it opens a slide-in drawer from the left with "Settings" item. Tapping Settings navigates to the settings screen. Drawer closes on swipe left or overlay tap. PagerView swipe still works from center of screen.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete settings flow</name>
  <files>invest-app/src/app/index.tsx, invest-app/src/app/settings.tsx</files>
  <action>Human verification of the complete settings UI and drawer navigation flow.</action>
  <verify>
    <automated>cd /Users/linmini/invest/invest-app && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Human has verified all settings UI elements work correctly and approved the checkpoint.</done>
  <what-built>Complete settings flow: hamburger drawer on home screen, settings screen with masked API key input (eye toggle, Test button, Clear button), model name field, base URL field, glow intensity pill selector. All values auto-save on blur with toast feedback.</what-built>
  <how-to-verify>
    1. Open the app on device/emulator
    2. Verify hamburger icon appears in top-left of home screen header
    3. Tap hamburger — drawer slides in from left with dimmed backdrop
    4. Tap outside drawer or swipe left — drawer closes
    5. Tap "Settings" in drawer — navigates to settings screen
    6. Verify settings screen has two sections: "API Configuration" and "Display"
    7. Type a test API key — verify it shows masked (dots + last 4 chars)
    8. Tap eye icon — verify full key is revealed; tap again to re-mask
    9. Tap away from API key field — verify "Saved" toast appears briefly
    10. Tap "Test" button — verify it attempts connection (will fail without real key, should show error feedback)
    11. Tap Clear/trash button — verify confirmation prompt appears
    12. Verify model name field shows "MiniMax-M2.5" default
    13. Verify base URL field shows "https://api.minimax.io/v1" default
    14. Verify glow intensity pills (Subtle/Medium/Heavy) with sliding highlight animation
    15. Tap Back — returns to home screen
    16. Verify PagerView swipe (home to AI analysis) still works from center of screen
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no type errors
2. All 5 new/modified files exist with expected exports
3. Hamburger drawer opens from home screen, navigates to settings
4. Settings screen has all required fields per SETT-05
5. API key is masked by default, revealable, auto-saves to SecureStore
</verification>

<success_criteria>
- Hamburger icon visible on home screen header (SETT-01)
- Settings screen shows API key, model name, base URL fields (SETT-05)
- API key is masked with reveal toggle (SETT-02 UI)
- Glow intensity pill selector works with sliding animation
- Drawer does not interfere with PagerView swipe navigation
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/05-settings/05-02-SUMMARY.md`
</output>
