---
phase: 06-ai-analysis
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - invest-app/src/features/analysis/components/AnalysisScreen.tsx
  - invest-app/src/features/analysis/components/AnalysisCard.tsx
  - invest-app/src/features/analysis/components/AnalysisSkeleton.tsx
  - invest-app/src/features/analysis/components/NoApiKeyPrompt.tsx
autonomous: true
requirements: [AI-02, AI-03, AI-04, AI-05, AI-06]

must_haves:
  truths:
    - "AnalysisScreen renders one AnalysisCard per watchlist stock"
    - "Each card collapsed shows stock code, name, current price, change, overallScore, Buy/Hold/Sell badge"
    - "Tapping a card expands it to show 4 sections: News Sentiment, Technical Analysis, Recommendation, Risk Assessment"
    - "Each section shows a score + text explanation"
    - "Skeleton shimmer cards shown while analysis is loading"
    - "Error card with Retry button shown when a stock's analysis fails"
    - "No-API-key screen shown with Settings navigation button when apiKey is empty"
    - "Sticky 'Not financial advice' disclaimer footer always visible"
  artifacts:
    - path: "invest-app/src/features/analysis/components/AnalysisScreen.tsx"
      provides: "Main analysis page component"
      exports: ["AnalysisScreen"]
    - path: "invest-app/src/features/analysis/components/AnalysisCard.tsx"
      provides: "Expandable stock analysis card"
      exports: ["AnalysisCard"]
    - path: "invest-app/src/features/analysis/components/AnalysisSkeleton.tsx"
      provides: "Shimmer skeleton placeholder"
      exports: ["AnalysisSkeleton"]
    - path: "invest-app/src/features/analysis/components/NoApiKeyPrompt.tsx"
      provides: "No-key CTA component"
      exports: ["NoApiKeyPrompt"]
  key_links:
    - from: "invest-app/src/features/analysis/components/AnalysisScreen.tsx"
      to: "invest-app/src/features/analysis/store/analysisStore.ts"
      via: "useAnalysisStore for cache/loading/errors"
      pattern: "useAnalysisStore"
    - from: "invest-app/src/features/analysis/components/AnalysisScreen.tsx"
      to: "invest-app/src/features/analysis/components/AnalysisCard.tsx"
      via: "renders one AnalysisCard per watchlist item"
      pattern: "AnalysisCard"
    - from: "invest-app/src/features/analysis/components/AnalysisCard.tsx"
      to: "invest-app/src/features/analysis/types.ts"
      via: "AnalysisResult type for card content"
      pattern: "import.*AnalysisResult"
---

<objective>
Build the AI analysis UI components: expandable cards with collapsed/expanded states, skeleton loading animation, error cards with retry, no-API-key prompt, and sticky disclaimer footer.

Purpose: Implements the visual layer that displays AI analysis results from Plan 01's store. Each card shows real TWSE data in the header (AI-07 compliance) and 4 analysis sections (AI-03 through AI-06).
Output: Four React components ready to be wired into the PagerView in Plan 03.
</objective>

<execution_context>
@/Users/linmini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/linmini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ai-analysis/06-RESEARCH.md
@.planning/phases/06-ai-analysis/06-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 -->

From invest-app/src/features/analysis/types.ts:
```typescript
export interface AnalysisResult {
  sentimentScore: number;        // 0-100
  sentimentLabel: 'Bullish' | 'Neutral' | 'Bearish';
  sentimentSummary: string;
  technicalSummary: string;
  recommendation: 'Buy' | 'Hold' | 'Sell';
  recommendationReasoning: string;
  riskScore: number;             // 0-100
  riskExplanation: string;
  overallScore: number;          // 0-100
}
```

From invest-app/src/features/analysis/store/analysisStore.ts:
```typescript
export const useAnalysisStore: {
  cache: Record<string, AnalysisResult>;
  loading: Record<string, boolean>;
  errors: Record<string, string | null>;
  fetchAnalysis: (symbol: string, quote: QuoteData, credentials: Credentials) => Promise<void>;
  clearAnalysis: () => void;
}
```

From invest-app/src/features/market/quoteStore.ts:
```typescript
interface Quote {
  symbol: string; name: string; price: number | null; prevClose: number;
  change: number; changePct: number; fetchedAt: number;
}
// useQuoteStore(s => s.quotes)
```

From invest-app/src/features/watchlist/store/watchlistStore.ts:
```typescript
export interface WatchlistItem {
  id: number; symbol: string; name: string; sort_order: number;
}
// useWatchlistStore(s => s.items)
```

From invest-app/src/features/settings/store/settingsStore.ts:
```typescript
// useSettingsStore(s => s.apiKey) — empty string means no key configured
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AnalysisSkeleton and NoApiKeyPrompt components</name>
  <files>
    invest-app/src/features/analysis/components/AnalysisSkeleton.tsx,
    invest-app/src/features/analysis/components/NoApiKeyPrompt.tsx
  </files>
  <action>
    **AnalysisSkeleton.tsx:**
    Create a shimmer skeleton card that mimics the collapsed AnalysisCard layout. Use Reanimated `withRepeat`/`withTiming` opacity pulse (1.0 to 0.3, duration 900ms, infinite reverse). Clean up animation with `cancelAnimation(opacity)` in useEffect return. Structure:
    - Outer container: `bg-surface border border-border rounded-lg p-4 mb-3`
    - Top row: two skeleton blocks (stock code area + score area)
    - Bottom row: one wide skeleton block (name/price area)
    - Each skeleton block: `Animated.View` with the pulsing opacity, `bg-border rounded` with fixed height/width

    **NoApiKeyPrompt.tsx:**
    Full-page centered prompt when no API key is configured. Per locked decision: clean CTA, no dimmed/blurred teaser.
    - Centered vertically and horizontally in flex-1 container
    - Icon or text header: "AI Analysis" in text-primary
    - Body: "Add your MiniMax API key to unlock AI analysis" in text-muted
    - Button: `Pressable` with `bg-primary rounded-lg px-6 py-3`, text "Go to Settings" in text-bg font-semibold
    - onPress: `useRouter().push('/settings')`
  </action>
  <verify>
    <automated>cd /Users/linmini/invest/invest-app && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>AnalysisSkeleton renders shimmer pulse animation with cleanup. NoApiKeyPrompt shows centered CTA with Settings navigation button.</done>
</task>

<task type="auto">
  <name>Task 2: AnalysisCard and AnalysisScreen components</name>
  <files>
    invest-app/src/features/analysis/components/AnalysisCard.tsx,
    invest-app/src/features/analysis/components/AnalysisScreen.tsx
  </files>
  <action>
    **AnalysisCard.tsx:**
    Expandable card component. Props: `{ symbol: string; name: string; quote: Quote; result: AnalysisResult | undefined; loading: boolean; error: string | null; onRetry: () => void }`.

    Collapsed state (always visible):
    - Stock code (symbol) + name in one row, `text-text font-bold`
    - Current price + change + changePct from quote prop (real TWSE data — satisfies AI-07)
    - Overall score (e.g. "72/100") with color coding: >=65 green (#00E676), >=40 amber (#FFB300), <40 red (#FF1744)
    - Buy/Hold/Sell pill badge with color: Buy=#00E676, Hold=#FFB300, Sell=#FF1744

    Expanded state (on tap):
    - Use Reanimated `useSharedValue` + `withTiming` for maxHeight animation (0 to 500, duration 250ms) with `overflow: 'hidden'`
    - Toggle `expanded` boolean state on Pressable tap
    - Four sections stacked vertically, each with header + score + text:
      1. "News Sentiment" — sentimentScore, sentimentLabel, sentimentSummary
      2. "Technical Analysis" — technicalSummary (no separate score)
      3. "Recommendation" — recommendation + recommendationReasoning
      4. "Risk Assessment" — riskScore + riskExplanation (risk color inverted: >=65 red, <40 green)

    Loading state: Show AnalysisSkeleton instead of result content (collapsed header still shows stock info from quote).

    Error state: Show error message in text-stock-down + "Retry" Pressable button. Call onRetry on press.

    Styling: `bg-surface border border-border rounded-lg p-4 mb-3`. Section dividers: `border-t border-border mt-3 pt-3`.

    **AnalysisScreen.tsx:**
    Main page component. Props: `{ isActive: boolean }`.

    Logic:
    1. Read `apiKey` from `useSettingsStore(s => s.apiKey)`. If empty, render `<NoApiKeyPrompt />` and return.
    2. Read `items` from `useWatchlistStore(s => s.items)`.
    3. Read `quotes` from `useQuoteStore(s => s.quotes)`.
    4. Read `cache, loading, errors, fetchAnalysis` from `useAnalysisStore`.
    5. `useEffect` with `[isActive]` dependency: when `isActive` becomes true, loop through items and call `fetchAnalysis(item.symbol, quotes[item.symbol], { apiKey, modelName, baseUrl })` for each. Use `useSettingsStore.getState()` for credentials inside the effect (not selector — avoids re-renders). Sequential calls (await each) to avoid MiniMax throttling.
    6. Render `ScrollView` with:
       - Header: "AI Analysis" in text-primary text-2xl font-bold
       - For each watchlist item: render `AnalysisCard` with symbol, name, quote, result from cache, loading state, error state, onRetry callback that calls fetchAnalysis for that symbol
       - Empty state if no watchlist items: "Add stocks to your watchlist first" in text-muted
    7. Sticky footer: "Not financial advice" disclaimer — use absolute positioning at bottom or a View after ScrollView. `text-muted text-xs text-center py-2 bg-bg border-t border-border`.

    IMPORTANT: Do NOT call fetchAnalysis from inside each AnalysisCard render. Call from AnalysisScreen useEffect only.
  </action>
  <verify>
    <automated>cd /Users/linmini/invest/invest-app && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>AnalysisCard shows collapsed stock info with expand/collapse animation revealing 4 analysis sections. AnalysisScreen renders cards for all watchlist stocks, shows skeleton during load, error with retry on failure, no-key prompt when apiKey empty, sticky disclaimer footer.</done>
</task>

</tasks>

<verification>
cd /Users/linmini/invest/invest-app && npx tsc --noEmit --pretty
</verification>

<success_criteria>
- AnalysisScreen renders one card per watchlist stock (AI-02)
- Each card shows sentiment score/label (AI-03), technical summary (AI-04), recommendation (AI-05), risk score (AI-06)
- Real TWSE price data visible in card header (AI-07 visual compliance)
- No-API-key screen navigates to Settings
- Skeleton loading with Reanimated shimmer
- Error per-stock with retry button
- "Not financial advice" sticky disclaimer visible
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-analysis/06-02-SUMMARY.md`
</output>
