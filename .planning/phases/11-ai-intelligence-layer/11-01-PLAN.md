---
phase: 11-ai-intelligence-layer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - invest-app/src/__tests__/patternDetector.test.ts
  - invest-app/src/features/charts/services/patternDetector.ts
  - invest-app/src/features/charts/components/PatternCard.tsx
  - invest-app/src/app/detail/[symbol].tsx
autonomous: true
requirements:
  - AI-11

must_haves:
  truths:
    - "PatternCard appears below volume bars on the stock detail page when a pattern is detected"
    - "PatternCard is completely hidden when no pattern matches (no placeholder row)"
    - "PatternCard re-evaluates and updates when the user switches timeframes"
    - "detectPatterns correctly identifies all 8 supported patterns from OHLCV data"
    - "detectPatterns returns the strongest pattern when multiple are detected in the same window"
    - "detectPatterns returns null and never throws for edge-case candles (body=0, gap candles)"
  artifacts:
    - path: "invest-app/src/__tests__/patternDetector.test.ts"
      provides: "Failing test stubs for all 8 pattern types, null return, strongest selection, NaN guard"
      exports: []
    - path: "invest-app/src/features/charts/services/patternDetector.ts"
      provides: "Pure TS pattern detector — detectPatterns(OHLCVPoint[]) returns PatternResult | null"
      exports: ["detectPatterns", "PatternResult", "PatternSignal"]
    - path: "invest-app/src/features/charts/components/PatternCard.tsx"
      provides: "UI card rendered below volume bars — pattern name, Chinese explanation, signal pill"
      exports: ["PatternCard"]
    - path: "invest-app/src/app/detail/[symbol].tsx"
      provides: "PatternCard inserted between VolumeBar block and TimeframeSelector/AlertStatusBar section"
      contains: "PatternCard"
  key_links:
    - from: "invest-app/src/app/detail/[symbol].tsx"
      to: "invest-app/src/features/charts/components/PatternCard.tsx"
      via: "import PatternCard; render between VolumeBar and TimeframeSelector"
      pattern: "PatternCard"
    - from: "invest-app/src/features/charts/components/PatternCard.tsx"
      to: "invest-app/src/features/charts/services/patternDetector.ts"
      via: "useMemo(() => detectPatterns(candles), [candles])"
      pattern: "detectPatterns"
    - from: "invest-app/src/features/charts/components/PatternCard.tsx"
      to: "invest-app/src/features/charts/store/chartStore.ts"
      via: "useChartStore(s => s.getCandles(symbol, timeframe))"
      pattern: "getCandles"
---

<objective>
Implement candlestick pattern recognition (AI-11): a pure TypeScript detector that identifies 8 supported patterns from OHLCV data, and a PatternCard component displayed below the volume bars on the stock detail page.

Purpose: Give users at-a-glance technical signals without any API call — fully offline, instant, zero token cost.
Output: patternDetector.ts service (pure TS), PatternCard.tsx component, detail screen wiring, passing tests.
</objective>

<execution_context>
@/Users/linmini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/linmini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ai-intelligence-layer/11-CONTEXT.md
@.planning/phases/11-ai-intelligence-layer/11-RESEARCH.md

<interfaces>
<!-- Key types the executor needs. Extracted from codebase — no exploration needed. -->

From invest-app/src/features/charts/types.ts:
```typescript
export interface OHLCVPoint {
  timestamp: number;
  open: number;
  high: number;
  low: number;
  close: number;
  volume: number;
}

export type Timeframe = '1D' | '5D' | '1M' | '6M' | '1Y';
```

From invest-app/src/features/charts/store/chartStore.ts:
```typescript
interface ChartState {
  cache: Record<string, OHLCVPoint[]>;
  loading: Record<string, boolean>;
  errors: Record<string, string | null>;
  fetchCandles: (symbol: string, timeframe: Timeframe) => Promise<void>;
  getCandles: (symbol: string, timeframe: Timeframe) => OHLCVPoint[] | undefined;
  clearCache: (symbol?: string) => void;
}
export const useChartStore = create<ChartState>(...);
```

From invest-app/src/features/analysis/components/AnalysisCard.tsx (score color pattern to reuse for signal pill):
```typescript
function scoreColor(score: number): string {
  if (score >= 65) return '#00E676';   // green / bullish
  if (score >= 40) return '#FFB300';   // amber / neutral
  return '#FF1744';                    // red / bearish
}
```

NativeWind cyberpunk tokens in use: `bg-surface`, `text-primary`, `border-border`, `text-muted`, `text-text`.

PatternCard insertion point in invest-app/src/app/detail/[symbol].tsx:
- VolumeBar block ends around line 200
- TimeframeSelector/AlertStatusBar section starts around line 207
- PatternCard goes between these two sections
- The detail screen already receives `symbol` and `timeframe` as state — pass both to PatternCard
- Consider wrapping the content area in a ScrollView (currently a plain View) to prevent overflow on small screens as content grows
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test stubs for patternDetector (Wave 0 — tests first)</name>
  <files>invest-app/src/__tests__/patternDetector.test.ts</files>
  <behavior>
    - Test: detectPatterns returns PatternResult with name='錘子', signal='bullish' for a hammer candle array (small body in upper third, lower shadow >= 2x body, bearish prior trend)
    - Test: detectPatterns returns PatternResult with name='倒錘子', signal='bullish' for inverted hammer
    - Test: detectPatterns returns PatternResult with name='吞噬多頭', signal='bullish' for bullish engulfing (prior bearish candle fully engulfed by current bullish)
    - Test: detectPatterns returns PatternResult with name='吞噬空頭', signal='bearish' for bearish engulfing
    - Test: detectPatterns returns PatternResult with name='恆星線', signal='bearish' for shooting star (long upper shadow, small body, bullish prior trend)
    - Test: detectPatterns returns PatternResult with name='十字星', signal='neutral' for doji (body <= 5% of range)
    - Test: detectPatterns returns PatternResult with name='早晨之星', signal='bullish' for morning star (3-candle: large bearish, doji/small, large bullish closing above midpoint of candle 1)
    - Test: detectPatterns returns PatternResult with name='黃昏之星', signal='bearish' for evening star (inverse)
    - Test: detectPatterns returns null when no pattern matches (random non-pattern candles)
    - Test: detectPatterns returns strongest (3-candle pattern over 1-candle) when both detected in same window
    - Test: detectPatterns returns null (not NaN or throw) when candle has open === close (body=0, zero-division guard)
    - Test: detectPatterns returns null for empty array input
  </behavior>
  <action>
    Create invest-app/src/__tests__/patternDetector.test.ts with all test cases listed in the behavior block. At this stage the file to be tested (patternDetector.ts) does not exist yet — all tests will be RED. Use concrete OHLCV fixture objects constructed to precisely match each pattern's detection rules (documented in 11-RESEARCH.md OHLCV Pattern Rule Reference table). No mocks needed — detectPatterns is a pure function.

    Run `cd invest-app && npm test -- --testPathPattern=patternDetector` after writing — confirm all tests are RED (import error is acceptable at this stage).
  </action>
  <verify>
    <automated>cd /Users/linmini/invest/invest-app && npm test -- --testPathPattern=patternDetector 2>&1 | tail -20</automated>
  </verify>
  <done>patternDetector.test.ts exists with all 12 test cases listed above; tests are RED (expected — implementation not yet written)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement patternDetector.ts and PatternCard.tsx</name>
  <files>
    invest-app/src/features/charts/services/patternDetector.ts,
    invest-app/src/features/charts/components/PatternCard.tsx
  </files>
  <behavior>
    - detectPatterns(candles) returns PatternResult | null
    - All 8 pattern detection rules pass the tests from Task 1
    - NaN guard: `const body = Math.abs(c.close - c.open); if (body < 0.01) treat as doji`
    - Confidence ranking: 3-candle patterns (早晨之星, 黃昏之星) confidence=0.9 > 2-candle (吞噬) confidence=0.7 > 1-candle (錘子, 恆星線) confidence=0.5 > 十字星 confidence=0.3
    - When multiple patterns found, return highest confidence; tie-break by most-recent candle index
    - PatternCard renders null when patternResult is null
    - PatternCard shows: pattern name (text-text, font-bold, 16px), explanation (text-muted, 12px), signal pill (colored bg matching bullish=#00E676/neutral=#FFB300/bearish=#FF1744 with matching text)
    - PatternCard uses useMemo(() => detectPatterns(candles), [candles]) to avoid re-render storm
  </behavior>
  <action>
    1. Create invest-app/src/features/charts/services/patternDetector.ts:
       - Export PatternSignal = 'bullish' | 'neutral' | 'bearish'
       - Export PatternResult interface: { name: string; explanation: string; signal: PatternSignal; confidence: number }
       - Define PATTERN_DEFS record with all 8 patterns (names and Chinese explanations from 11-RESEARCH.md Pattern 1 code block)
       - Implement detectPatterns(candles: OHLCVPoint[]): PatternResult | null
       - Each rule function takes the relevant trailing candles (last 1, 2, or 3) and returns confidence or 0
       - Use the OHLCV threshold values from the OHLCV Pattern Rule Reference table in 11-RESEARCH.md
       - Body-zero guard: any candle where Math.abs(close-open) < 0.01 is treated as a doji body for ratio purposes
       - Collect all matches, sort by confidence desc, return top result or null
       - No React imports — pure TypeScript, no side effects

    2. Create invest-app/src/features/charts/components/PatternCard.tsx:
       - Props: { candles: OHLCVPoint[] | undefined; }
       - If candles is undefined or candles.length < 3, return null
       - Compute pattern via useMemo
       - If pattern is null, return null (no placeholder)
       - Render a View with className="mx-4 mb-3 bg-surface border border-border rounded-lg p-3"
         containing: pattern name (Text className="text-text font-bold text-base"), explanation (Text className="text-muted text-xs mt-1"), signal pill (View with inline style backgroundColor matching signal color, rounded-full px-2 py-0.5, Text white text-xs)
       - Signal pill label: '看漲' for bullish, '中性' for neutral, '看跌' for bearish

    Run `cd invest-app && npm test -- --testPathPattern=patternDetector` — all 12 tests must be GREEN.
  </action>
  <verify>
    <automated>cd /Users/linmini/invest/invest-app && npm test -- --testPathPattern=patternDetector 2>&1 | tail -20</automated>
  </verify>
  <done>All 12 patternDetector tests GREEN; patternDetector.ts and PatternCard.tsx exist with correct exports; no TypeScript errors (tsc --noEmit passes)</done>
</task>

<task type="auto">
  <name>Task 3: Wire PatternCard into the stock detail screen</name>
  <files>invest-app/src/app/detail/[symbol].tsx</files>
  <action>
    Modify invest-app/src/app/detail/[symbol].tsx to add PatternCard between the VolumeBar block and the TimeframeSelector/AlertStatusBar section:

    1. Import PatternCard from '../features/charts/components/PatternCard'
    2. Import useChartStore if not already imported (it is used for fetchCandles/getCandles)
    3. After the VolumeBar rendering block and before the TimeframeSelector section, add:
       ```tsx
       <PatternCard candles={useChartStore.getState().getCandles(symbol, selectedTimeframe)} />
       ```
       Note: use the store's getCandles method with the current symbol and selectedTimeframe state values. The candles update automatically when fetchCandles resolves because chartStore triggers re-render via Zustand subscription — use the hook form: `const candles = useChartStore(s => s.getCandles(symbol, selectedTimeframe))` inside the component, then pass as prop. This means PatternCard re-evaluates on every timeframe switch automatically.

    4. If the content area below the chart is a plain View (not ScrollView), wrap it in a ScrollView to prevent overflow on small screens as more content is added. Keep the chart itself (CandleChart + VolumeBar) outside the ScrollView — only the items below (PatternCard, TimeframeSelector, AlertStatusBar) should scroll.

    Run full test suite: `cd invest-app && npm test` — all existing 241+ tests must still pass (detail screen is not unit-tested, so no new test failures expected from this wiring change).
  </action>
  <verify>
    <automated>cd /Users/linmini/invest/invest-app && npm test 2>&1 | tail -15</automated>
  </verify>
  <done>detail/[symbol].tsx imports and renders PatternCard with current candles; tsc --noEmit passes; full test suite green; PatternCard position is between VolumeBar and TimeframeSelector in JSX</done>
</task>

</tasks>

<verification>
Full suite: `cd /Users/linmini/invest/invest-app && npm test` — all tests green including the 12 new patternDetector tests.
TypeScript: `cd /Users/linmini/invest/invest-app && npx tsc --noEmit` — zero errors.
Pattern detector smoke: `cd /Users/linmini/invest/invest-app && npm test -- --testPathPattern=patternDetector -- --verbose` — all 12 cases individually named and green.
</verification>

<success_criteria>
- patternDetector.test.ts: 12 tests, all GREEN
- patternDetector.ts: exports detectPatterns, PatternResult, PatternSignal; pure TS no React
- PatternCard.tsx: renders null when no pattern, renders card with name + explanation + signal pill when pattern detected
- detail/[symbol].tsx: PatternCard rendered between VolumeBar and TimeframeSelector using current symbol + selectedTimeframe
- No regression: full test suite remains green (241+ tests)
- tsc --noEmit: zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-ai-intelligence-layer/11-01-SUMMARY.md` summarizing:
- Files created/modified
- Pattern detection rules implemented and threshold values chosen
- PatternCard rendering approach
- Any deviations from the plan
- Test results (count, pass/fail)
</output>
