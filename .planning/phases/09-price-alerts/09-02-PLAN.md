---
phase: 09-price-alerts
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - invest-app/src/features/alerts/components/AlertModal.tsx
  - invest-app/src/features/alerts/components/AlertsListModal.tsx
  - invest-app/src/features/alerts/components/AlertStatusBar.tsx
  - invest-app/src/app/detail/[symbol].tsx
  - invest-app/src/app/index.tsx
  - invest-app/src/app/_layout.tsx
  - invest-app/src/app/settings.tsx
  - invest-app/src/features/market/quoteStore.ts
autonomous: true
requirements:
  - ALRT-01
  - ALRT-03
  - ALRT-06

must_haves:
  truths:
    - "Bell icon on detail screen header opens bottom sheet modal for setting upper/lower price alerts"
    - "Alert status bar below chart shows current alert state ('up-arrow 980 Active / down-arrow 920 Triggered') and tapping opens edit modal"
    - "Bell icon on home screen header with badge count opens modal listing all alerts across stocks"
    - "Foreground quote poll triggers alert condition checks every 30s"
    - "Battery optimization link available in Settings screen"
    - "Notification channels created at app startup in _layout.tsx"
    - "alertStore hydrated from SQLite on app start"
  artifacts:
    - path: "invest-app/src/features/alerts/components/AlertModal.tsx"
      provides: "Bottom sheet for creating/editing per-stock alerts"
      min_lines: 60
    - path: "invest-app/src/features/alerts/components/AlertsListModal.tsx"
      provides: "All-alerts overview modal from home bell icon"
      min_lines: 50
    - path: "invest-app/src/features/alerts/components/AlertStatusBar.tsx"
      provides: "Inline alert status below chart on detail screen"
      min_lines: 20
  key_links:
    - from: "invest-app/src/app/detail/[symbol].tsx"
      to: "invest-app/src/features/alerts/components/AlertModal.tsx"
      via: "bell icon press opens AlertModal"
      pattern: "AlertModal"
    - from: "invest-app/src/app/index.tsx"
      to: "invest-app/src/features/alerts/components/AlertsListModal.tsx"
      via: "bell icon press opens AlertsListModal"
      pattern: "AlertsListModal"
    - from: "invest-app/src/features/market/quoteStore.ts"
      to: "invest-app/src/features/alerts/services/alertMonitor.ts"
      via: "checkAlerts called after each quote poll tick"
      pattern: "checkAlerts"
    - from: "invest-app/src/app/_layout.tsx"
      to: "invest-app/src/features/alerts/services/alertTask.ts"
      via: "top-level import for task registration"
      pattern: "alertTask"
---

<objective>
Wire the alert UI components into the existing screens and integrate foreground alert checking into the quote poll cycle. Add notification channel setup and alertStore hydration at app startup.

Purpose: All data layer from Plan 01 needs UI surfaces and app lifecycle integration to become user-facing features.
Output: Working alert creation from detail screen, alert management from home screen, foreground price monitoring, battery optimization in Settings.
</objective>

<execution_context>
@/Users/linmini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/linmini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-price-alerts/09-CONTEXT.md
@.planning/phases/09-price-alerts/09-RESEARCH.md
@.planning/phases/09-price-alerts/09-01-SUMMARY.md

@invest-app/src/app/detail/[symbol].tsx
@invest-app/src/app/index.tsx
@invest-app/src/app/_layout.tsx
@invest-app/src/app/settings.tsx
@invest-app/src/features/market/quoteStore.ts

<interfaces>
<!-- From Plan 01 outputs — executor should use these directly -->

From invest-app/src/features/alerts/store/alertStore.ts:
```typescript
export const useAlertStore = create<AlertState>((set, get) => ({
  alerts: AlertRow[],
  loadFromDb: () => Promise<void>,
  upsertAlert: (symbol, name, upper, lower) => Promise<void>,
  deleteAlert: (id) => Promise<void>,
  reEnable: (symbol, direction) => Promise<void>,
  getBySymbol: (symbol) => AlertRow | undefined,
  activeCount: () => number,
}));
```

From invest-app/src/features/alerts/services/alertService.ts:
```typescript
export interface AlertRow {
  id: number;
  symbol: string;
  name: string;
  upper_price: number | null;
  lower_price: number | null;
  upper_status: 'active' | 'triggered';
  lower_status: 'active' | 'triggered';
}
```

From invest-app/src/features/alerts/services/alertMonitor.ts:
```typescript
export async function checkAlerts(quotes: Record<string, QuoteLike>): Promise<void>;
```

From invest-app/src/features/alerts/services/alertTask.ts:
```typescript
export const ALERT_CHECK_TASK: string;
export async function registerAlertTask(): Promise<void>;
export async function unregisterAlertTask(): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alert UI components and detail/home screen integration</name>
  <files>
    invest-app/src/features/alerts/components/AlertModal.tsx,
    invest-app/src/features/alerts/components/AlertsListModal.tsx,
    invest-app/src/features/alerts/components/AlertStatusBar.tsx,
    invest-app/src/app/detail/[symbol].tsx,
    invest-app/src/app/index.tsx
  </files>
  <action>
1. **AlertModal.tsx** — Bottom sheet modal for creating/editing per-stock alerts. Props: `{ visible, onClose, symbol, name, currentPrice }`. Uses React Native Modal with `animationType="slide"` and `transparent`. Content:
   - Two optional TextInput fields: "Upper target" and "Lower target" (numeric keyboard)
   - Pre-fill: if alert exists for symbol (via `useAlertStore.getBySymbol`), show existing values. If new, pre-fill upper at `Math.ceil(currentPrice * 1.05)`, lower at `Math.floor(currentPrice * 0.95)`.
   - Each field has a clear button (X) to make that direction null
   - Validation: upper must be > currentPrice (show red text if not), lower must be < currentPrice
   - "Save" button calls `useAlertStore.upsertAlert(symbol, name, upper, lower)` then `onClose()`
   - Request notification permission on first save: `Notifications.requestPermissionsAsync()` — if denied, show inline warning "Notification permission required for alerts"
   - Cyberpunk styling: bg-surface, border-border, text-text, text-primary accents

2. **AlertsListModal.tsx** — Modal listing all alerts across all stocks. Props: `{ visible, onClose }`. Uses Modal with slide animation.
   - Read `useAlertStore(s => s.alerts)`, separate into active (upper_status=active OR lower_status=active) and triggered (both triggered or one triggered + other null)
   - Active section on top, triggered section below (dimmed with opacity-50)
   - Each row: `${name} ↑${upper_price} ${upper_status} / ↓${lower_price} ${lower_status}`
   - Swipe-to-delete using `ReanimatedSwipeable` (same pattern as watchlist). On swipe open: `useAlertStore.deleteAlert(id)`
   - Triggered alerts show "Re-enable" pressable for each triggered direction. On press: `useAlertStore.reEnable(symbol, direction)`
   - Empty state: "No alerts set"

3. **AlertStatusBar.tsx** — Inline component for detail screen showing current alert state. Props: `{ symbol, onPress }`. Reads alert from `useAlertStore.getBySymbol(symbol)`. If no alert exists, render nothing. If alert exists, show a pressable row: `↑980 Active / ↓920 Triggered` (use stock-up/primary for active, muted for triggered). `onPress` opens the edit modal.

4. **Detail screen** — Modify `invest-app/src/app/detail/[symbol].tsx`:
   - Add bell icon (Unicode bell character or Text "Alert") to the header right section (next to price display). On press, open AlertModal.
   - Add `AlertStatusBar` below the timeframe selector. Pass `onPress` to re-open AlertModal for editing.
   - Manage `alertModalVisible` state.
   - Import AlertModal, AlertStatusBar, useAlertStore.

5. **Home screen** — Modify `invest-app/src/app/index.tsx` WatchlistPage:
   - Add bell icon with badge in the header row (between hamburger and "Watchlist" title, or to the right of "+ Add"). Use `useAlertStore(s => s.activeCount())` for badge number.
   - On press, open AlertsListModal.
   - Manage `alertsListVisible` state.
   - Import AlertsListModal, useAlertStore.
  </action>
  <verify>
    <automated>cd /Users/linmini/invest/invest-app && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Bell icon visible on detail screen header, tapping opens AlertModal with upper/lower price inputs
    - AlertModal pre-fills with +5%/-5% for new alerts, existing values for edits
    - Validation prevents upper <= currentPrice or lower >= currentPrice
    - AlertStatusBar shows below chart when alerts exist for the stock
    - Bell icon with badge on home screen header, tapping opens AlertsListModal
    - AlertsListModal shows active and triggered alerts with swipe-to-delete and re-enable
  </done>
</task>

<task type="auto">
  <name>Task 2: App lifecycle wiring — notifications, foreground checks, background task, battery settings</name>
  <files>
    invest-app/src/app/_layout.tsx,
    invest-app/src/app/settings.tsx,
    invest-app/src/features/market/quoteStore.ts
  </files>
  <action>
1. **_layout.tsx** — Three additions:
   a) Import `'../features/alerts/services/alertTask'` at top level (bare import, no named exports needed — this ensures `TaskManager.defineTask` runs at module evaluation time). This is CRITICAL — the import must be unconditional and at file top, not inside a component.
   b) In the migration-success useEffect (after watchlist loadFromDb): call `useAlertStore.getState().loadFromDb()` to hydrate alerts from SQLite.
   c) Add notification channel setup: import `* as Notifications from 'expo-notifications'`. In the migration-success useEffect, create two channels:
      - `setNotificationChannelAsync('price-alerts', { name: 'Price Alerts', importance: AndroidImportance.HIGH, vibrationPattern: [0, 250, 250, 250], sound: 'default' })`
      - `setNotificationChannelAsync('monitoring-status', { name: 'Monitoring Status', importance: AndroidImportance.LOW })`
   d) Set notification foreground handler: `Notifications.setNotificationHandler({ handleNotification: async () => ({ shouldShowBanner: true, shouldShowList: true, shouldPlaySound: true, shouldSetBadge: false }) })` — place this OUTSIDE the component (module level), before RootLayout function.

2. **quoteStore.ts** — Add foreground alert checking. In the `tick` function, after `set({ quotes: { ...get().quotes, ...quotes } })` (the main polling success path, NOT the market-close path), add:
   ```
   import { checkAlerts } from '../alerts/services/alertMonitor';
   // After setting quotes:
   checkAlerts(quotes).catch(() => {});
   ```
   The `.catch(() => {})` ensures a failed alert check never breaks quote polling. Pass the `quotes` record (keyed by symbol) that was just constructed. Note: the `quotes` variable in tick already has the right shape `Record<string, Quote>` which satisfies `QuoteLike`.

3. **settings.tsx** — Add a "Battery Optimization" row in a new "Alerts" section below the Display section. Import `startActivityAsync, ActivityAction` from `expo-intent-launcher` and `Platform` from `react-native`. Only render on Android (`Platform.OS === 'android'`). Section header: "Alerts" (same uppercase tracking-widest style). Row: Pressable with "Battery Optimization" title, subtitle "Disable for reliable background price alerts", right side shows "Open Settings" in text-primary. On press: `startActivityAsync(ActivityAction.IGNORE_BATTERY_OPTIMIZATION_SETTINGS)`.
  </action>
  <verify>
    <automated>cd /Users/linmini/invest/invest-app && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - alertTask.ts imported at _layout.tsx top level (background task registered at module eval)
    - Notification channels 'price-alerts' and 'monitoring-status' created at app startup
    - alertStore hydrated from SQLite during app init
    - quoteStore tick() calls checkAlerts after each successful poll
    - Settings screen has Battery Optimization link that opens Android system settings
    - TypeScript compiles with no errors
  </done>
</task>

</tasks>

<verification>
- `cd invest-app && npx tsc --noEmit` passes with zero errors
- `cd invest-app && npx jest --no-coverage` full test suite passes
- Detail screen shows bell icon and AlertStatusBar
- Home screen shows bell icon with badge
- Settings screen shows Battery Optimization row
- _layout.tsx imports alertTask at top level
- quoteStore.ts calls checkAlerts after polling tick
</verification>

<success_criteria>
- All alert UI components render correctly on their target screens
- Alert creation flow: bell icon -> modal -> set prices -> save -> status bar visible
- Alert management flow: home bell -> list modal -> swipe delete / re-enable
- Foreground alert checking runs every 30s via quoteStore poll
- Background task registered at module level, ready for WorkManager
- Notification channels created at startup
- Battery optimization accessible from Settings
</success_criteria>

<output>
After completion, create `.planning/phases/09-price-alerts/09-02-SUMMARY.md`
</output>
